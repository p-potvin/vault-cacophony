# CrispASR carried-patch manifest
#
# Every line is a patch this fork carries on top of upstream ggml, expressed as
# an executable check so that "MUST RE-APPLY after ggml bump" is enforced by CI
# instead of remembered by a human. It exists because it was NOT remembered: a
# core_adaln ARM failure survived for weeks because the vendored tree predated
# two upstream SVE fixes, and nobody was watching.
#
# Format:  <min-count> <TAB> <file> <TAB> <fixed-string pattern>
# A patch whose count drops below <min-count> fails the build.
#
# When an upstream merge legitimately removes or renames a patch, update this
# file IN THE SAME COMMIT and say why in the message. Editing it to make CI
# green without that justification defeats the entire point.

# --- new ops we add to the ordered GGML_OP_* enum -------------------------
1	include/ggml.h	GGML_OP_NORM_AFFINE
1	include/ggml.h	GGML_OP_COL2IM_1D
1	include/ggml.h	GGML_OP_AA_SNAKE_BETA
1	include/ggml.h	ggml_norm_affine
1	include/ggml.h	ggml_aa_snake_beta
2	src/ggml.c	GGML_OP_COUNT == 103

# --- issue #38: F16 vec_dot_type = F32 to avoid saturating F32->F16 -------
# vfmaq_f16 accumulates in an F16 register and overflows at 65504, which turns
# long F16xF16 dots into +-Inf -> NaN. Upstream has NOT fixed this.
1	src/ggml-cpu/ggml-cpu.c	ggml_vec_dot_f16_f32
1	src/ggml-cpu/ggml-cpu.c	issue #38
1	src/ggml-cpu/vec.h	ggml_vec_dot_f16_f32
1	src/ggml-cpu/simd-mappings.h	GGML_F32Cx4_FMA
# the conv im2col patch buffer is mul_mat's src1, so it follows vec_dot_type
2	src/ggml-cpu/ops.cpp	vec_dot_type

# --- carried CPU / backend ops -------------------------------------------
1	src/ggml-cpu/ops.h	ggml_compute_forward_norm_affine
1	src/ggml-cpu/ops.h	ggml_compute_forward_aa_snake_beta

# --- Metal ----------------------------------------------------------------
1	src/ggml-metal/ggml-metal.metal	aa_snake_beta
1	src/ggml-metal/ggml-metal-ops.cpp	aa_snake_beta
1	src/ggml-metal/ggml-metal-device.cpp	aa_snake_beta
1	src/ggml-metal/ggml-metal-device.m	crispasr_metal_pipeline_cache
# NOTE: NORM_AFFINE is deliberately CPU + CUDA only — there is no Metal kernel,
# so do not add one here expecting it to exist.

# --- Vulkan ---------------------------------------------------------------
1	src/ggml-vulkan/ggml-vulkan.cpp	pipeline_col2im_1d_f32
1	src/ggml-vulkan/ggml-vulkan.cpp	conv_transpose_1d_f16
1	src/ggml-vulkan/ggml-vulkan.cpp	pipeline_snake_f32

# --- CUDA -----------------------------------------------------------------
1	src/ggml-cuda/im2col.cu	CrispASR patch
1	src/ggml-cuda/norm.cu	CrispASR patch
1	src/ggml-cuda/ggml-cuda.cu	CrispASR patch

# --- CUDA build robustness -------------------------------------------------
# Upstream links the CUDA::cuda_driver imported target unconditionally for the
# VMM pool; it is not always defined (Kaggle, nvidia/cuda devel containers).
1	src/ggml-cuda/CMakeLists.txt	GGML_CUDA_DRIVER_LIB
